import CommunityRecipes from "@/components/Community/Community";
export const dynamic = "force-dynamic";

const page = () => {
  return (
    <div>
      <CommunityRecipes />
    </div>
  );
};

export default page;
