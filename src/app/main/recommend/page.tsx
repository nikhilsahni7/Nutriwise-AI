import MealRecommendations from "@/components/Recommendation/MealRecommendation";

export const dynamic = "force-dynamic";

const page = () => {
  return (
    <div>
      <MealRecommendations />
    </div>
  );
};

export default page;
