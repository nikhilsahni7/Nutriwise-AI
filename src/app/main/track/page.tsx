import MealTracker from "@/components/Tracking/Tracker";
export const dynamic = "force-dynamic";

const page = () => {
  return (
    <div>
      <MealTracker />
    </div>
  );
};

export default page;
